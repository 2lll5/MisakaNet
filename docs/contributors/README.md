# Contributor point details

This directory is generated by `python3 scripts/contributor_points.py build`.
Each contributor receives `<username>.html` with their event-by-event point breakdown after their
first accepted point event.
